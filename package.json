{
  "name": "qu4sar-spa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5500",
    "build": "tsc && vite build && node -e \"require('fs').writeFileSync('dist/404.html', require('fs').readFileSync('dist/index.html'))\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dom-to-image-more": "^3.10.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vite": "^6"
  }
}
